module.exports = {
  commit: root => {
    if (root._commit) return root._commit()
    return root.emit('succeeded')
  },
  rollback: root => {
    if (root._rollback) return root._rollback()
    return root.emit('failed')
  }
}
